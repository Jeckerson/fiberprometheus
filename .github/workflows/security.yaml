on:
  push:

name: Security
jobs:
  Gosec:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run Gosec
        uses: securego/gosec@master
        with:
          args: ./...
